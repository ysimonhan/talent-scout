# Evals

This folder holds reusable behavioral checks that sit above unit tests.

- `smoke/`: fast scenarios that prove the main path still works
- future project-specific eval sets can live beside it
